{
    "sseAddr": ":8080",
    "sseUrl": "http://localhost:8080/test",
    "mcpServers": {
        "filesystem": {
          "transport": "stdio",
          "cmd": "npx -y @modelcontextprotocol/server-filesystem /Users/tiankai1/",
          "env": []
        }
    }
}